# Assets

Architecture diagrams and supporting visuals for the blog post.

## Files

- `architecture-diagram.svg` — (TODO: export the SVG from the index.html inline diagram as a standalone file)

## Notes

The index.html contains an inline SVG architecture diagram. For the published blog, this should be exported as a standalone SVG or PNG for the `assets/` folder and referenced via `![Architecture](assets/architecture-diagram.svg)` in the README.
